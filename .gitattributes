# Normalise line endings so the repo is identical regardless of the OS it was
# committed from. Without this, a Windows checkout stores CRLF and every file
# shows as fully modified when opened on Linux or macOS.
* text=auto eol=lf

*.png binary
*.pbix binary
*.db binary
*.graphml text eol=lf
